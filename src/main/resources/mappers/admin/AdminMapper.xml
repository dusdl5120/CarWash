<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.green.carwash.dao.admin.AdminMapper">

	<select id="noticeListPage" resultType="kr.green.carwash.vo.admin.AdminBoardVO">
		SELECT 
			*
		FROM
			board_carwash
		ORDER BY ID DESC
		LIMIT #{pageStart}, #{perPageNum}
	</select>
	
	<select id="countNotice" resultType="int">
		SELECT
			COUNT(*)
		FROM 
			board_carwash
	</select>

	<insert id="noticeInsert" parameterType="kr.green.carwash.vo.admin.AdminBoardVO"> 
		INSERT INTO board_carwash (
			title
			, contents
			, registered_id
			, registered_date
		)VALUES(
			#{title}
			, #{contents}
			, #{registered_id}
			, now()
		)
			
	</insert>


	<select id="noticeRead" resultType="kr.green.carwash.vo.admin.AdminBoardVO">
		select
			id 
			, title
			, contents
			, registered_id
			, registered_date
		from
			board_carwash
		where
			id = #{id}
	</select>

	<update id="noticeUpdate">
		update
			board_carwash
		set
			title = #{title}
			, contents = #{contents}
			, registered_id = #{registered_id}
			, registered_date = now()
			, updated_id = #{registered_id}
			, updated_date = now()
		where
			id = #{id}
	</update>














</mapper>